<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Outer Circle -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@color/purple" /> <!-- Change this color as needed -->
        </shape>
    </item>

    <!-- Inner Circle (to create the ring effect) -->
    <item
        android:left="20dp"
        android:right="20dp"
        android:top="20dp"
        android:bottom="20dp">
        <shape android:shape="oval">
            <solid android:color= "@color/white"/> /> <!-- Change this color as needed -->
        </shape>
    </item>

</layer-list>